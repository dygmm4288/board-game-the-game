import { Button, Flex } from "@radix-ui/themes";

const RoomBtns = () => {
  return (
    <Flex gap='2' direction='row'>
      <Button onClick={}>방 만들기</Button>
      <Button onClick={}>코드로 참여</Button>
    </Flex>
  );
};

export default RoomBtns;
